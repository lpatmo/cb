<span class="heading">
<a href="{{profileUrl}}">{{postAuthorName}}</a>
has proposed a new study session:
{{#if url}}
  <a href="{{postUrl}}" class="action-link">{{postTitle}}}</a>
{{else}}
  <a href="{{postUrl}}">{{postTitle}}}</a>
{{/if}}
</span><br><br>

{{#if htmlBody}}
  <div class="post-body">
  {{{htmlBody}}}
  </div>
  <br>
{{/if}}

<a href="{{postUrl}}">Discuss</a><br><br>
